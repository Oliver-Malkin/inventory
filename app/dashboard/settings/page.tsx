import { Metadata } from "next";

export const metadata: Metadata = {
  title: 'Settings',
};

export default function Dashboard() {
  return (
    <main>
      <h1>Settings Page</h1>
    </main>
  );
}
